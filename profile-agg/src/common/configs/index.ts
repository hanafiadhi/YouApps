import AppConfig from './app.config';
import RmqConfig from './rmq.config';
import SwaggerConfig from './swagger.config';

export { AppConfig, RmqConfig, SwaggerConfig };
export default [AppConfig, RmqConfig, SwaggerConfig];
